import { HeadPagePipPipe } from './head-page-pip.pipe';

describe('HeadPagePipPipe', () => {
  it('create an instance', () => {
    const pipe = new HeadPagePipPipe();
    expect(pipe).toBeTruthy();
  });
});
